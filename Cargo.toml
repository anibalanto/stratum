[workspace]
members = [
    "crates/stratum",
    "crates/stratum-cli",
]
resolver = "2"

# Una sola fuente de verdad: cada crate la hereda con `license.workspace = true`.
[workspace.package]
license = "MIT OR Apache-2.0"
